<!---On use _ pour différencier les includes-->

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">ActuNews POO</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item =">
                    <a class="nav-link" href="{{ path('default_index') }}">Accueil</a>
                </li>
                {% for category in categories %}
                    <li class="nav-item =">
                        <a class="nav-link" href="{{ path('default_category', {alias : category.alias}) }}">{{ category.name}}</a>
                    </li>
                {% endfor %}

            </ul>
        </div>
        <div class="justify-content-end">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('app_logout') }}" class=" btn btn-dark  text-white">DECONNEXION</a>
            {% else %}
                <a href="{{ path('app_login') }}" class=" btn btn-primary text-white">CONNEXION</a>
                <a href="{{ path('user_register') }}" class=" btn btn-info text-white ml-2">INSCRIPTION</a>
            {% endif %}

        </div>

    </nav>
